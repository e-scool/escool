{{!-- <%= header_title %> --}}
{{!-- <%= render partial: 'shared/header/buttons' %> --}}

{{!-- <div class="header-buttons">
  <% if current_user.parent? %>
    <button data-toggle="dropdown">Nen</button>
    <ul class="dropdown-menu">
      <% if current_user.children.count > 1 %>
        <% current_user.children.each do |c| %>
          <% unless c.id == current_user.current_child_id %>
            <li><a href="/update-child/<%= c.id %>"><i class="icon-user"></i> <%= c.name %></a></li>
          <% end %>
        <% end %>
        <li class="divider"></li>
      <% end %>
      <li><a href="<%= destroy_user_session_path %>" data-method="delete"><i class="icon-off"></i> <%= t "devise.links.logout" %></a></li>
    </ul>
  <% else %>
    <button data-toggle="dropdown">Classe</button>
    <ul class="dropdown-menu">
      <% if current_user.classrooms.count > 1 %>
        <% current_user.classrooms.by_name.each do |c| %>
          <li><a href="/update-classroom/<%= c.id %>"><i class="icon-user"></i> <%= c.name %></a></li>
        <% end %>
        <li class="divider"></li>
      <% end %>
      <li>
        <a href="<%= destroy_user_session_path %>" data-method="delete"><i class="icon-off"></i> <%= t "devise.links.logout" %></a>
      </li>
    </ul>
  <% end %>
  <a href="<%= edit_user_registration_path %>" class="button">El meu perfil</a>
</div> --}}