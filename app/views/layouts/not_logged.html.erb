<!DOCTYPE html>
  <head>
    <title>e-Scool</title>
    <link rel="shortcut icon" href="<%= asset_path('favicon.ico') %>" />

    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <link rel="stylesheet" href="ie.css" type="text/css" media="screen, projection" />
    <![endif]-->

    <link href='http://fonts.googleapis.com/css?family=Scada:400,700' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag 'application-app', media: 'all' %>

    <%= javascript_include_tag 'translations' %>

    <script type="text/javascript" charset="utf-8">
      I18n.defaultLocale = "<%= I18n.default_locale %>";
      I18n.locale = "<%= I18n.locale %>";
      I18n.fallbacks = true;
    </script>

    <%= javascript_include_tag 'not_logged' %>

    <%= yield :jsheader %>

    <%= csrf_meta_tags %>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>
    <div class="not-logged-wrapper">
      <div class="not-logged-top">
        <a href="<%= root_url %>" class="logo"><img src="<%= asset_path('logo.png') %>" /></a>
      </div>
      <div class="not-logged-container">
        <%= render :partial => 'layouts/messages' %>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
