<p class="title"><%= t("login") %></p>

<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :login, :input_html => {:class => "input-block-level"}, label: false, :required => false, :autofocus => true %>
    <%= f.input :password, :input_html => {:class => "input-block-level"}, label: false, :required => false %>
    <%= f.check_box :remember_me, :as => :boolean if devise_mapping.rememberable? %>
    <%= f.label :remember_me, :class => 'remember_me' if devise_mapping.rememberable? %>
    <%= f.button :submit, "Entrar" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
